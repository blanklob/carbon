section.banner(role="section")
  .container
    .banner-wrapper
      .banner__content
        h1.banner__title How your tweets are impacting the planet ?
        p.text-big.banner__description Estimate your Twitter account carbon footprint by using our carbon calculator, check it out now.
      .banner__search
        form.search__form(role="form")
          label.search__label(for="username") Your Twitter Username
          input.search__input(type="text", autocomplete="off", id="username", placeholder="Twitter username...")
          .hide-below-sm
            +button('search__btn', 'Calculate')
          img.search__icon.show-below-sm(src="search.svg", alt="Search Icon")
        p.text-small.search__disclaimer.hide-below-sm *Please note that we are a sustainable website and we never store your informations.

